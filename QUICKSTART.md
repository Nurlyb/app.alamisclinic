# 🚀 Quick Start Guide

## Быстрый старт за 5 минут

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка окружения

```bash
cp .env.example .env
```

Отредактируйте `.env` файл с вашими настройками:
- `DATABASE_URL` - строка подключения к PostgreSQL
- `JWT_SECRET` - секретный ключ для JWT
- `REDIS_URL` - URL Redis сервера

### 3. Запуск базы данных (Docker)

```bash
docker-compose up -d postgres redis
```

### 4. Применение миграций

```bash
npm run prisma:generate
npm run prisma:migrate
```

### 5. Заполнение тестовыми данными

```bash
npm run prisma:seed
```

### 6. Запуск приложения

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

## 🔑 Тестовые учётные данные

Все пользователи имеют пароль: `clinic123`

| Роль | Телефон | Имя |
|------|---------|-----|
| Владелец | +77001234567 | Владимир Петров |
| Оператор | +77001234568 | Анна Смирнова |
| Ресепшн | +77001234569 | Мария Иванова |
| Ассистент | +77001234570 | Елена Козлова |
| Доктор | +77001234571 | Ван Инь Ся |
| Доктор | +77001234572 | Нурузова Зарина |
| Доктор | +77001234573 | Алексей Петров |

## 📡 Тестирование API

### Вход в систему

```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "+77001234571",
    "password": "clinic123"
  }'
```

### Получение расписания

```bash
curl http://localhost:3000/api/appointments?date=2026-02-26 \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

### Создание записи

```bash
curl -X POST http://localhost:3000/api/appointments \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "patientId": "PATIENT_ID",
    "doctorId": "DOCTOR_ID",
    "serviceId": "SERVICE_ID",
    "departmentId": "DEPARTMENT_ID",
    "datetime": "2026-02-27T10:00:00Z",
    "comment": "Первичный осмотр"
  }'
```

## 🔧 Полезные команды

```bash
# Разработка
npm run dev                 # Запуск в режиме разработки
npm run build              # Сборка для production
npm run start              # Запуск production сборки

# База данных
npm run prisma:studio      # Открыть Prisma Studio
npm run prisma:migrate     # Применить миграции
npm run prisma:seed        # Заполнить тестовыми данными
npm run prisma:reset       # Сбросить БД и применить миграции

# Код
npm run lint               # Проверка кода
npm run format             # Форматирование кода
```

## 🐳 Docker

Запуск всего приложения в Docker:

```bash
docker-compose up -d
```

Это запустит:
- PostgreSQL на порту 5432
- Redis на порту 6379
- Приложение на порту 3000

## 📚 Дополнительная документация

- [API Documentation](./API.md) - Полная документация API
- [Deployment Guide](./DEPLOYMENT.md) - Руководство по развёртыванию
- [README](./README.md) - Общая информация о проекте

## ❓ Проблемы?

### База данных не подключается

Проверьте, что PostgreSQL запущен:
```bash
docker-compose ps
```

### Ошибка миграций

Сбросьте базу данных:
```bash
npm run prisma:reset
```

### Порт занят

Измените порт в `.env`:
```
PORT=3001
```

## 🎉 Готово!

Теперь вы можете:
1. Войти в систему через API
2. Создавать записи на приём
3. Управлять пациентами
4. Генерировать чеки
5. Просматривать аналитику

Для полной документации см. [API.md](./API.md)
