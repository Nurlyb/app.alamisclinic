# 🚀 Инструкция по развертыванию на сервере

## Ваш сервер
- **IP**: 185.129.49.186
- **Домен**: app.alamisclinic.kz
- **Конфигурация**: 2 vCPU / 4GB RAM / 100GB Storage

---

## 📋 Что нужно сделать

### 1️⃣ Настроить DNS (СНАЧАЛА!)

Зайдите в панель управления доменом на **hoster.kz** и добавьте A-запись:

```
Тип записи: A
Имя хоста: app
IP адрес: 185.129.49.186
TTL: 3600 (или оставьте по умолчанию)
```

После добавления подождите 5-10 минут для распространения DNS.

---

### 2️⃣ Подключиться к серверу

Откройте терминал и выполните:

```bash
ssh root@185.129.49.186
```

Введите пароль от сервера.

---

### 3️⃣ Запустить автоматическую установку

Скопируйте и выполните эти команды по очереди:

```bash
# Скачать скрипт установки
curl -o deploy.sh https://raw.githubusercontent.com/Nurlyb/app.alamisclinic/main/deploy.sh

# Дать права на выполнение
chmod +x deploy.sh

# Запустить установку
./deploy.sh
```

Скрипт автоматически:
- ✅ Установит Node.js, PostgreSQL, Redis, Nginx
- ✅ Создаст базу данных
- ✅ Склонирует проект с GitHub
- ✅ Установит все зависимости
- ✅ Настроит и запустит приложение
- ✅ Настроит Nginx

**Время установки**: 5-10 минут

---

### 4️⃣ Установить SSL сертификат

После завершения установки выполните:

```bash
apt install -y certbot python3-certbot-nginx
certbot --nginx -d app.alamisclinic.kz
```

Следуйте инструкциям certbot:
1. Введите email для уведомлений
2. Согласитесь с условиями (Y)
3. Выберите опцию 2 (перенаправление на HTTPS)

---

## ✅ Готово!

Приложение доступно по адресу: **https://app.alamisclinic.kz**

---

## 🔐 Тестовые учетные данные

Для входа используйте:

| Роль | Телефон | Пароль |
|------|---------|--------|
| Владелец | +77001234567 | clinic123 |
| Оператор | +77001234568 | clinic123 |
| Регистратор | +77001234569 | clinic123 |
| Доктор | +77001234570 | clinic123 |

---

## 🔧 Полезные команды

### Проверить статус приложения
```bash
pm2 status
```

### Посмотреть логи
```bash
pm2 logs alamis-clinic
```

### Перезапустить приложение
```bash
pm2 restart alamis-clinic
```

### Обновить приложение (после изменений в GitHub)
```bash
cd /var/www/app.alamisclinic
git pull
npm install
npx prisma migrate deploy
npm run build
pm2 restart alamis-clinic
```

---

## 🆘 Если что-то пошло не так

### Приложение не запускается
```bash
# Посмотрите логи
pm2 logs alamis-clinic --lines 50

# Перезапустите
pm2 restart alamis-clinic
```

### Не открывается сайт
```bash
# Проверьте Nginx
nginx -t
systemctl status nginx

# Перезапустите Nginx
systemctl restart nginx
```

### База данных не работает
```bash
# Проверьте PostgreSQL
systemctl status postgresql

# Перезапустите
systemctl restart postgresql
```

---

## 📚 Дополнительная документация

- **Полная инструкция**: [DEPLOY_SERVER.md](./DEPLOY_SERVER.md)
- **Быстрый старт**: [QUICK_DEPLOY.md](./QUICK_DEPLOY.md)
- **Основная документация**: [README.md](./README.md)

---

## 📞 Контакты

Если возникнут вопросы, проверьте логи приложения командой:
```bash
pm2 logs alamis-clinic
```

Все логи сохраняются в `/var/www/app.alamisclinic/logs/`
