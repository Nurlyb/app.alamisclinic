# 🔄 Обновление на сервере (исправление JWT)

## Выполните на сервере:

```bash
cd /var/www/app.alamisclinic && git pull origin main && npm run build && pm2 restart alamis-clinic && pm2 save && pm2 status
```

## Что было исправлено:
- Убраны явные type casts `as jwt.Secret` из констант JWT
- Константы теперь имеют автоматический вывод типов
- TypeScript компилятор теперь корректно определяет типы

## После выполнения проверьте:
```bash
pm2 logs alamis-clinic --lines 30
```
