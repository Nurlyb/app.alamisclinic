# 🚀 Команды для выполнения на сервере

## ✅ Что уже сделано локально:
- Исправлены все TypeScript ошибки сборки
- Создан `ecosystem.config.js` для PM2
- Код закоммичен и запушен в GitHub
- Сборка проекта завершена успешно

## 📋 Выполните на сервере (185.129.49.186):

### Одной командой (скопируйте и вставьте):

```bash
cd /var/www/app.alamisclinic && git pull origin main && npm run build && pm2 restart alamis-clinic && pm2 save && pm2 status
```

### Или пошагово:

```bash
# 1. Перейти в директорию
cd /var/www/app.alamisclinic

# 2. Обновить код из GitHub
git pull origin main

# 3. Собрать проект
npm run build

# 4. Перезапустить PM2
pm2 restart alamis-clinic

# 5. Сохранить конфигурацию PM2
pm2 save

# 6. Проверить статус
pm2 status
```

## 🔍 Проверка после обновления:

```bash
# Просмотр логов (последние 30 строк)
pm2 logs alamis-clinic --lines 30

# Проверка API
curl http://localhost:3000/api/health

# Проверка процесса
pm2 status
```

## ✨ Ожидаемый результат:

- PM2 статус: `online` ✅
- Логи: без ошибок ✅
- API health check: 200 OK ✅
- Приложение доступно на: http://app.alamisclinic.kz ✅

## 🆘 Если возникли проблемы:

```bash
# Полный перезапуск
pm2 delete alamis-clinic
pm2 start ecosystem.config.js
pm2 save

# Просмотр полных логов
pm2 logs alamis-clinic --lines 100

# Проверка порта
netstat -tulpn | grep 3000
```

---

**Готово к запуску!** 🎉
