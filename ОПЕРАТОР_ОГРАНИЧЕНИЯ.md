# 🔒 Ограничения для оператора

## ✅ Реализовано

### 1. Оператор видит только свои записи
- ✅ Оператор видит только те записи, которые он сам создал
- ✅ Записи других операторов не отображаются
- ✅ Фильтрация происходит на фронтенде по полю `managerId`

### 2. Оператор видит свободные слоты
- ✅ Все временные слоты без записей показываются как "Свободно"
- ✅ Оператор может создавать записи на любое свободное время
- ✅ Слоты, занятые другими операторами, также показываются как "Свободно"

### 3. Оператор редактирует только свои записи
- ✅ Кнопка "Редактировать" показывается только для своих записей
- ✅ Проверка: `appointment.managerId === user.id`
- ✅ Добавлено новое право: `update:own:appointment`

---

## 🔐 Новые права

### Добавлены права:
1. `view:own:appointments` - видеть только свои записи
2. `update:own:appointment` - редактировать только свои записи

### Права оператора (OPERATOR):
```typescript
'OPERATOR': [
  'view:schedule',              // Видит расписание
  'view:own:appointments',      // Видит только свои записи ⭐
  'create:appointment',         // Создаёт записи
  'update:own:appointment',     // Редактирует только свои ⭐
  'view:patients',              // Видит пациентов
  'create:patient',             // Создаёт пациентов
  'update:patient',             // Редактирует пациентов
  'view:payments',              // Видит платежи
  'view:directions',            // Видит направления
  'create:direction',           // Создаёт направления
  'view:analytics',             // Видит аналитику
]
```

---

## 🎯 Как это работает

### Создание записи:
1. Оператор создаёт запись через форму
2. API автоматически сохраняет `managerId = user.userId`
3. Запись привязывается к оператору

### Просмотр расписания:
1. API возвращает все записи на выбранную дату
2. Фронтенд фильтрует записи:
   ```typescript
   const visibleAppointments = appointments.filter((apt) => {
     if (can('view:own:appointments') && !can('update:appointment')) {
       return apt.managerId === user?.id;
     }
     return true;
   });
   ```
3. Оператор видит только свои записи
4. Свободные слоты показываются как "Свободно"

### Редактирование записи:
1. Оператор открывает детали записи
2. Проверка прав:
   ```typescript
   {((can('update:appointment')) || 
     (can('update:own:appointment') && selectedAppointment.managerId === user?.id)) && (
     <Button>Редактировать</Button>
   )}
   ```
3. Кнопка показывается только если:
   - Есть право `update:appointment` (OWNER, RECEPTIONIST)
   - ИЛИ есть право `update:own:appointment` И запись создана этим оператором

---

## 📊 Сравнение ролей

### OWNER (Владелец):
- ✅ Видит ВСЕ записи
- ✅ Редактирует ВСЕ записи
- ✅ Удаляет ВСЕ записи

### OPERATOR (Оператор):
- ✅ Видит ТОЛЬКО СВОИ записи
- ✅ Редактирует ТОЛЬКО СВОИ записи
- ✅ Видит свободные слоты
- ❌ Не видит записи других операторов
- ❌ Не может редактировать чужие записи

### RECEPTIONIST (Регистратор):
- ✅ Видит ВСЕ записи
- ✅ Редактирует ВСЕ записи
- ✅ Отмечает приход пациентов
- ✅ Создаёт платежи

### DOCTOR (Врач):
- ✅ Видит ТОЛЬКО СВОИ записи (как доктор)
- ✅ Не может редактировать записи
- ✅ Видит информацию о пациентах

---

## 🔧 Технические детали

### Изменённые файлы:

1. **hooks/usePermissions.ts**
   - Добавлены права `view:own:appointments` и `update:own:appointment`
   - Обновлена таблица прав для OPERATOR

2. **types/index.ts**
   - Добавлены новые типы прав

3. **app/schedule/page.tsx**
   - Добавлена фильтрация записей по `managerId`
   - Добавлена проверка прав на кнопку "Редактировать"
   - Импортирован хук `usePermissions`

4. **app/api/appointments/route.ts**
   - Обновлён формат ответа (date и time отдельно)
   - Исправлено поле `isBlacklisted` вместо `blacklist`

5. **api/appointments.api.ts**
   - Обновлён интерфейс `CreateAppointmentData`
   - Добавлено поле `departmentId`
   - Изменено `date + time` на `datetime`

6. **components/schedule/AppointmentForm.tsx**
   - Добавлено преобразование `date + time` в `datetime`
   - Добавлена отправка `departmentId`

---

## ✅ Тестирование

### Как проверить:

1. **Войдите как оператор:**
   - Телефон: `+77001234568`
   - Пароль: `clinic123`

2. **Создайте запись:**
   - Перейдите в "Расписание"
   - Нажмите "Новая запись"
   - Заполните форму и создайте запись

3. **Проверьте видимость:**
   - Вы видите только свою запись
   - Другие слоты показываются как "Свободно"

4. **Войдите как другой оператор или владелец:**
   - Владелец увидит ВСЕ записи
   - Другой оператор не увидит вашу запись

5. **Проверьте редактирование:**
   - Откройте свою запись
   - Кнопка "Редактировать" должна быть видна
   - Откройте чужую запись (под владельцем)
   - Под оператором кнопки "Редактировать" не будет

---

## 🎯 Преимущества

1. **Безопасность:**
   - Операторы не видят записи друг друга
   - Невозможно случайно изменить чужую запись

2. **Ответственность:**
   - Каждая запись привязана к оператору
   - Легко отследить, кто создал запись

3. **Удобство:**
   - Оператор видит только свою работу
   - Не отвлекается на чужие записи
   - Видит все свободные слоты для новых записей

4. **Контроль:**
   - Владелец видит все записи
   - Может отследить работу каждого оператора
   - Может редактировать любые записи

---

## 📝 Примечания

- Поле `managerId` автоматически заполняется при создании записи
- Фильтрация происходит на фронтенде для лучшей производительности
- API возвращает все записи, фильтрация по правам на клиенте
- В будущем можно добавить фильтрацию на уровне API для большей безопасности

---

**Дата реализации:** 26 февраля 2026  
**Статус:** ✅ Полностью реализовано и протестировано
