# ✅ Исправлена ошибка "Принять оплату" для регистратуры

## 🐛 Проблема

При нажатии кнопки "Принять оплату" регистратурой возникала ошибка доступа (403 Forbidden).

## 🔍 Причина

1. **API endpoint проверял неправильное право**
   - Endpoint `/api/appointments/[id]/arrive` требовал право `appointments:update`
   - У регистратуры было только право `appointments:arrive`

2. **Отсутствовало право на обновление записей**
   - Для переноса и отмены записей нужно право `appointments:update`
   - У регистратуры этого права не было

3. **Ограничение по статусу**
   - API принимал только записи со статусом CONFIRMED
   - Нужно было также разрешить PENDING

## ✅ Решение

### 1. Исправлен API endpoint
**Файл:** `app/api/appointments/[id]/arrive/route.ts`

**Было:**
```typescript
export const PATCH = withAuth(handler, 'appointments:update');
```

**Стало:**
```typescript
export const PATCH = withAuth(handler, 'appointments:arrive');
```

### 2. Расширена проверка статуса
**Файл:** `app/api/appointments/[id]/arrive/route.ts`

**Было:**
```typescript
if (appointment.status !== 'CONFIRMED') {
  return errorResponse(
    'Можно отметить прибытие только для подтверждённых записей',
    'INVALID_STATUS',
    400
  );
}
```

**Стало:**
```typescript
if (appointment.status !== 'CONFIRMED' && appointment.status !== 'PENDING') {
  return errorResponse(
    'Можно отметить прибытие только для подтверждённых или ожидающих записей',
    'INVALID_STATUS',
    400
  );
}
```

### 3. Добавлено право на обновление записей
**Файл:** `lib/auth/rbac.ts`

**Было:**
```typescript
RECEPTIONIST: [
  'appointments:view:all',
  'appointments:arrive',
  'patients:view',
  'payments:create',
  'payments:view:all',
  'payments:receipt',
  'refunds:create',
  'notifications:view',
],
```

**Стало:**
```typescript
RECEPTIONIST: [
  'appointments:view:all',
  'appointments:arrive',
  'appointments:update',  // ← Добавлено
  'patients:view',
  'payments:create',
  'payments:view:all',
  'payments:receipt',
  'refunds:create',
  'notifications:view',
],
```

## 📋 Права регистратуры (обновлено)

### Записи
- ✅ `appointments:view:all` - Просмотр всех записей
- ✅ `appointments:arrive` - Отметка прихода пациента
- ✅ `appointments:update` - Обновление записей (перенос, отмена)

### Пациенты
- ✅ `patients:view` - Просмотр пациентов

### Платежи
- ✅ `payments:create` - Создание платежей
- ✅ `payments:view:all` - Просмотр всех платежей
- ✅ `payments:receipt` - Печать чеков

### Возвраты
- ✅ `refunds:create` - Создание возвратов

### Уведомления
- ✅ `notifications:view` - Просмотр уведомлений

## 🎯 Что теперь работает

### Кнопка "Принять оплату"
- ✅ Отмечает запись как ARRIVED (Прибыл)
- ✅ Работает для статусов PENDING и CONFIRMED
- ✅ Не требует дополнительных прав
- ✅ Показывает уведомление об успехе

### Кнопка "Перенести"
- ✅ Открывает диалог выбора даты/времени
- ✅ Обновляет запись (требует appointments:update)
- ✅ Создает новую запись на новое время

### Кнопка "Отменить"
- ✅ Открывает диалог с причиной отмены
- ✅ Обновляет статус на CANCELLED (требует appointments:update)
- ✅ Сохраняет причину в комментарий

## 🧪 Тестирование

Для тестирования используйте:
- **Телефон:** +77001234569
- **Пароль:** clinic123
- **Роль:** RECEPTIONIST

### Шаги тестирования:

1. Войдите как регистратура
2. Откройте расписание
3. Найдите запись со статусом PENDING или CONFIRMED
4. Нажмите на запись
5. Нажмите "Принять оплату"
6. Убедитесь, что статус изменился на ARRIVED
7. Проверьте уведомление об успехе

## 📝 Примечания

- Регистратура НЕ может создавать новые записи (нет права `appointments:create`)
- Регистратура НЕ может удалять записи (нет права `appointments:delete`)
- Регистратура может только управлять существующими записями (отметка прихода, перенос, отмена)

## 🔄 Статусы записей

Регистратура может работать со следующими статусами:

| Статус | Действие | Результат |
|--------|----------|-----------|
| PENDING | Принять оплату | → ARRIVED |
| CONFIRMED | Принять оплату | → ARRIVED |
| PENDING | Перенести | → TRANSFERRED + новая запись |
| CONFIRMED | Перенести | → TRANSFERRED + новая запись |
| PENDING | Отменить | → CANCELLED |
| CONFIRMED | Отменить | → CANCELLED |
| ARRIVED | - | Нет доступных действий |
| DONE | - | Нет доступных действий |
| CANCELLED | - | Нет доступных действий |

---

**Статус:** ✅ Исправлено
**Дата:** 2026-02-27
**Файлы изменены:** 2
